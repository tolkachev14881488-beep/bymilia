name: Live site check

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  live-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run live site tests
        run: node scripts/live-site-test.mjs https://by-milia.by
